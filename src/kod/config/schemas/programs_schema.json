{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Kodos Program Configuration Schema",
  "description": "Schema for the 'programs' section in kod config.lua",
  "type": "object",
  "properties": {
    "programs": {
      "type": "object",
      "description": "Configurable programs to install",
      "additionalProperties": {
        "type": "object",
        "description": "Program name maps to configuration dict"
      }
    }
  }
}
